# Tango 要件定義書

## 0. 文書情報

| 項目 | 内容 |
|---|---|
| プロダクト名 | Tango |
| 文書種別 | Webアプリ要件定義書（Codex引き継ぎ用） |
| 作成日 | 2026-08-20 |
| 元会話 | ChatGPT「要件整理 Bок」 |
| 対象 | MVPおよび将来拡張 |

### 0.1 この文書で使う状態

- **決定済み**: 会話内でユーザーが明示した、または後続の会話で採用された仕様。
- **設計方針**: 決定済み要件を実現するため、会話内で合意方向になった構成。
- **要確認**: 提案は出ているが、採用・詳細が確定していない事項。Codexは独断で固定しないこと。
- **将来**: MVPには含めず、後続バージョンで検討する事項。

## 1. プロダクト概要

Tangoは、ユーザーが主に英単語と日本語の意味を登録し、後から文字入力テストで復習できる個人向け単語帳Webアプリである。

中心となる価値は次の3点である。

1. 英単語1件に対して複数の日本語の意味を登録できる。
2. 回答履歴から正解率を計算し、単語カードの色で習熟状況を直感的に把握できる。
3. 完全一致・正規化一致・AI意味判定を組み合わせ、厳密さと実用性を両立する。

## 2. 目的と対象範囲

### 2.1 目的

- 好きな英単語を自分専用の単語帳へ登録できること。
- 登録した単語を繰り返しテストできること。
- 苦手な単語を見つけやすくし、優先的に復習できること。
- Googleアカウントでログインし、別端末でも同じデータを利用できること。
- 将来、Chrome拡張からも同じ単語データへ登録できる構成にしておくこと。

### 2.2 想定ユーザー

- 英語ドキュメントやWebページを読む中で、知らない英単語を自分用に蓄積したいユーザー。
- 選択式ではなく、日本語の意味を自分で入力して覚えたいユーザー。

### 2.3 MVPで対象外とするもの

- Chrome拡張そのものの実装。
- ネイティブのスマートフォンアプリ。
- タグ機能。
- 例文、品詞、発音記号の管理。
- 高度な学習分析ダッシュボード。
- CSV/JSONの一括インポート・エクスポート。

## 3. 決定済み仕様の要約

| 分類 | 決定内容 |
|---|---|
| 認証 | Better Authを使用し、Googleログインを提供する。利用にはログインを必須とする。 |
| データ分離 | 単語と回答履歴をユーザーIDに紐付け、ユーザー間で分離する。 |
| 単語 | 主に英単語を登録する。登録・一覧・編集・削除を提供する。 |
| 意味 | 1つの英単語に1件以上の日本語の意味を登録できる。 |
| 許容回答 | 独立した「許容回答」機能は設けない。複数の意味として扱う。 |
| ヒント | 単語ごとに0件または1件のヒントを登録できる。 |
| 翻訳 | 単語登録画面に翻訳ボタンを置き、英単語から日本語の意味入力を補助する。 |
| テスト | 登録単語を表示し、ユーザーが日本語の意味を文字入力して回答する。 |
| 出題方式 | 完全ランダムと苦手優先の2モードを提供する。 |
| 正誤判定 | 完全一致、正規化一致、AI意味判定の順で判定する。 |
| 履歴 | 回答、正誤、判定種別、ヒント利用有無を保存する。 |
| 可視化 | 正解率と正解数/回答数を表示し、正解率に応じてカード色を変える。未回答は白とする。 |
| 基盤 | React、TypeScript、TanStack Start、Hono、Cloudflare Workers、D1、Drizzleを中心に構成する。 |
| 将来拡張 | Webページで選択した英単語を「Tangoに登録」できるChrome拡張を想定する。 |

## 4. 機能要件

### 4.1 認証

#### AUTH-001 Googleログイン

- Better Authを使用する。
- Google OAuthによるログインを提供する。
- MVPではGoogleログインを認証手段とする。

#### AUTH-002 ログイン必須

- 未ログインユーザーは、単語一覧、登録、編集、削除、テスト、履歴へアクセスできない。
- 未ログイン時はログイン画面へ誘導する。

#### AUTH-003 ユーザーデータの分離

- 単語、意味、回答履歴は認証済みユーザーのIDに紐付ける。
- APIはリクエスト本文のユーザーIDを信用せず、認証セッションから対象ユーザーを決定する。
- ログイン後は、別端末でも同じ登録単語と履歴を参照できる。

### 4.2 単語の登録・管理

#### WORD-001 単語登録

- ユーザーは単語を登録できる。
- MVPの主対象は英単語とする。
- 単語1件につき、意味を1件以上必須とする。
- ヒントは任意入力とする。

#### WORD-002 複数意味

- 英単語1件に対し、日本語の意味を複数登録できる。
- 意味は追加・編集・削除できる。
- 最後の1件の意味は削除できない、または保存時に1件以上を必須とする。
- 「許容回答」という別概念・別入力欄は設けない。

例:

```text
issue
  - 問題
  - 論点
  - 発行物
  - 発行する
```

#### WORD-003 一覧

- 自分が登録した単語だけを一覧表示する。
- 各単語には、少なくとも次を表示する。
  - 単語
  - 登録された意味
  - 正解率
  - 正解数 / 回答数
  - 習熟状況を表すカード色
  - 編集操作
  - 削除操作
- 未回答の単語は正解率を0%として扱わず、「未回答」として区別する。

#### WORD-004 編集・削除

- ユーザーは自分の単語、意味、ヒントを編集できる。
- ユーザーは自分の単語を削除できる。
- 他ユーザーの単語は参照・編集・削除できない。
- 削除時の回答履歴の扱いは未確定事項とする。

#### WORD-005 登録画面の統計表示

- 単語の新規登録フォームには回答回数を表示しない。
- 単語一覧や学習状況を確認する場所には、正解率と正解数/回答数を表示する。

### 4.3 ヒント

#### HINT-001 ヒント登録

- 単語1件につき、ヒントを0件または1件登録できる。
- タグの代替として、学習時に思い出すきっかけとなる自由記述を保存する。

#### HINT-002 テスト時の表示

- ヒントが設定されている問題だけ「ヒントを見る」ボタンを表示する。
- ボタンを押すまではヒント本文を表示しない。
- ボタンを押すと、登録済みヒントを問題画面内に表示する。

#### HINT-003 利用履歴

- 回答ごとにヒントを使用したかを `hint_used` として保存する。
- MVPでは、ヒントを使用して正解した場合も通常の正解として正解率へ含める。
- 将来、ノーヒント正解率を算出できるよう、履歴データは保持する。

### 4.4 翻訳

#### TRANS-001 翻訳ボタン

- 単語登録・編集画面の単語入力欄付近に「翻訳する」ボタンを配置する。
- 主な翻訳方向は英語から日本語とする。

#### TRANS-002 登録前の補助

- 翻訳結果は、即座にDBへ保存しない。
- 翻訳結果を意味の入力候補としてフォームへ反映する。
- ユーザーは候補を確認し、必要に応じて編集・削除・追加してから単語を保存する。
- 複数候補を返せる場合は、登録したい意味を複数選択できる形を想定する。

#### TRANS-003 翻訳サービスの分離

- アプリケーションから翻訳プロバイダーを直接呼ぶ実装に固定せず、バックエンドに `TranslationService` 相当の境界を設ける。
- Workers AI、DeepL、Google Translationなどへ差し替えられる設計とする。
- MVPで使用する翻訳プロバイダーは未確定とする。

### 4.5 テスト

#### TEST-001 テスト開始

- テスト開始前に、出題方式を選択できる。
- 選択肢は次の2つとする。
  - 完全ランダム
  - 苦手優先
- 出題数の選択肢と既定値は未確定とする。

#### TEST-002 完全ランダム

- 登録済み単語からランダムに出題する。
- 同一テスト内での重複出題の可否は未確定とする。

#### TEST-003 苦手優先

- 正解率が低い単語ほど出題されやすくする。
- 苦手な単語だけに限定せず、全単語に出題可能性を残す。
- MVPは正解率を主な重みとしてよい。
- 未回答単語、回答回数、直近の正誤をどう重みに反映するかは未確定とする。

#### TEST-004 回答

- 問題として英単語を1件表示する。
- ユーザーは日本語の意味を文字入力する。
- 回答送信後に正誤判定を行い、結果を保存する。
- AI判定を使用した場合は、そのことを結果表示で明示する。

#### TEST-005 回答後の表示

- 少なくとも次を表示する。
  - 正解または不正解
  - ユーザーの回答
  - 登録されている意味
  - 使用された判定種別
- 次の問題へ進める操作を提供する。

### 4.6 正誤判定

#### JUDGE-001 判定順序

各登録意味に対し、次の順序で判定する。

```text
ユーザー回答
  ↓
1. 登録された意味のいずれかと完全一致
  ↓ 不一致
2. 登録された意味のいずれかと正規化一致
  ↓ 不一致
3. AIによる意味判定
  ↓
正解 / 不正解
```

いずれか1つの登録意味に当てはまれば正解とする。

#### JUDGE-002 完全一致

- 入力文字列と登録意味がそのまま一致した場合は正解とする。
- 判定種別は `exact` とする。

#### JUDGE-003 正規化一致

比較前に少なくとも次の正規化を行う。

- 前後の空白を除去する。
- 大文字・小文字の差を無視する。
- 全角・半角を統一する。
- 連続する空白を統一する。

正規化後にいずれかの登録意味と一致した場合は正解とし、判定種別を `normalized` とする。

句読点、記号、かな・カナ、長音、表記ゆれをどこまで正規化するかは未確定とする。

#### JUDGE-004 AI意味判定

- 完全一致・正規化一致のどちらにも該当しない場合だけAI判定を呼び出す。
- AIには、英単語、登録された複数意味、ユーザー回答を渡し、意味として一致するかを判定させる。
- 判定種別は `ai` とする。
- AIプロバイダー、モデル、プロンプト、タイムアウト時の挙動は未確定とする。

#### JUDGE-005 許容回答の廃止

- `accepted` という判定種別は使用しない。
- `acceptable_answers` テーブルや許容回答入力欄は作らない。
- 正解として扱いたい別の日本語表現は、その単語の意味として登録する。

### 4.7 回答履歴と可視化

#### HISTORY-001 回答履歴

回答ごとに少なくとも次を保存する。

- ユーザーID
- 単語ID
- ユーザー回答
- 最終的な正誤
- 判定種別（`exact` / `normalized` / `ai`）
- ヒント利用有無
- 回答日時

#### HISTORY-002 正解率

- 単語ごとに `正解数 / 回答数` で正解率を算出する。
- 回答履歴が0件の場合は正解率0%ではなく未回答として扱う。
- ヒント利用回答もMVPでは通常の回答として集計する。

#### HISTORY-003 カード色

- 未回答のカードは白色とする。
- 回答済みカードは、0%に近いほど赤、100%に近いほど黄緑にする。
- 0%から100%まで連続的に色が変化する表現を基本方針とする。
- 正確なカラーコード、コントラスト、段階数は未確定とする。

## 5. 画面要件

### 5.1 ログイン画面

表示要素:

- Tangoの名称またはロゴ。
- アプリの短い説明。
- 「Googleでログイン」ボタン。

動作:

- 認証成功後、単語一覧へ遷移する。
- 認証済みユーザーがアクセスした場合は単語一覧へ遷移する。

### 5.2 単語一覧画面

表示要素:

- 登録単語のカード一覧。
- 各カード上の単語、複数の意味、正解率、正解数/回答数、編集、削除。
- 正解率と連動した背景色。未回答は白。
- 単語登録画面への導線。
- テスト開始画面への導線。

UI方針:

- 単語帳らしいカードデザインとする。
- 色だけに依存せず、正解率を文字でも表示する。
- モバイル幅でも主要操作が可能なレスポンシブUIを目標とする。ただし対応ブレークポイントは未確定。

### 5.3 単語登録・編集画面

表示要素:

- 単語入力欄。
- 翻訳ボタン。
- 1件以上の意味入力欄。
- 意味の追加・削除操作。
- 任意のヒント入力欄。
- 保存、キャンセル。

動作:

- 翻訳ボタンは意味候補をフォームへ反映するだけで、保存は行わない。
- ユーザーが内容を確認・編集して保存する。
- 新規登録フォームでは回答回数を表示しない。

### 5.4 テスト設定画面

表示要素:

- 完全ランダム / 苦手優先の選択。
- テスト開始ボタン。
- 出題数選択は要確認。

### 5.5 テスト問題画面

表示要素:

- 現在の英単語。
- 日本語の意味を入力する回答欄。
- 回答ボタン。
- ヒント登録済みの場合のみ「ヒントを見る」ボタン。
- 問題番号/総問題数は、出題数仕様の確定後に表示する。

動作:

- ヒントボタン押下時にヒント本文を表示し、当該回答の `hint_used` を記録する。
- 回答送信後は二重送信を防止する。

### 5.6 回答結果画面または問題画面内の結果領域

表示要素:

- 正誤。
- ユーザー回答。
- 登録された複数の意味。
- 完全一致、正規化一致、AI判定のどれで判定されたか。
- AI判定時はAIが判定したことを明示する。
- 次の問題へ進む操作。

### 5.7 テスト終了結果画面

この画面は有力なMVP追加案だが、採用範囲の最終確認が必要である。

候補要素:

- 正解数 / 出題数。
- 今回の正解率。
- 間違えた単語一覧。
- ヒントを使った単語一覧。
- 間違えた単語だけ再テストする操作。

## 6. 主要フロー

### 6.1 単語登録

```text
ログイン
  ↓
単語登録画面を開く
  ↓
英単語を入力
  ↓
必要なら翻訳を実行
  ↓
意味候補を確認・編集し、1件以上選択または入力
  ↓
必要ならヒントを入力
  ↓
保存
  ↓
単語一覧へ反映
```

### 6.2 テスト回答

```text
出題方式を選択
  ↓
英単語を表示
  ↓
必要ならヒントを表示
  ↓
日本語の意味を入力
  ↓
完全一致 → 正規化一致 → AI判定
  ↓
履歴を保存
  ↓
正誤・登録意味・判定種別を表示
  ↓
次の問題へ
```

## 7. 技術構成

### 7.1 採用方針

| レイヤー | 技術 |
|---|---|
| 言語 | TypeScript |
| Web UI | React |
| フルスタックフレームワーク | TanStack Start |
| ルーティング | TanStack Router |
| サーバー状態管理 | TanStack Query |
| API | Hono REST API |
| 実行環境・ホスティング | Cloudflare Workers |
| データベース | Cloudflare D1 |
| ORM | Drizzle ORM |
| 認証 | Better Auth + Google OAuth |
| AI/翻訳 | Cloudflare Workers AIを候補とし、プロバイダー交換可能にする |

ライブラリの具体的なバージョン、Cloudflare上での最新の互換性、デプロイ設定は実装開始時に公式ドキュメントで再確認する。

### 7.2 論理アーキテクチャ

```text
                   +-------------------+
                   | Web               |
                   | TanStack Start    |
                   +---------+---------+
                             |
                             |
                   +---------v---------+
                   | Hono REST API     |
                   | Cloudflare Worker |
                   +---+---------+-----+
                       |         |
             +---------v--+   +--v----------------+
             | Better Auth|   | Application       |
             | Google OAuth|  | Services          |
             +------------+   +--+-------------+--+
                                 |             |
                           +-----v----+   +----v-------+
                           | D1       |   | AI /       |
                           | Drizzle  |   | Translation|
                           +----------+   +------------+

将来:
Chrome Extension ---------> 同じHono REST API
```

### 7.3 API設計方針

- Webと将来のChrome拡張は同じAPIを利用する。
- 認証・認可はAPI境界で必ず検証する。
- UIからD1を直接操作しない。
- 翻訳とAI正誤判定はサービス境界を分ける。
- 具体的なURL、HTTPメソッド、エラー形式、バージョニングは詳細設計で確定する。

想定する機能単位:

- 認証セッション取得。
- 単語一覧取得、登録、更新、削除。
- 翻訳候補取得。
- テスト問題取得または出題セッション作成。
- 回答送信と正誤判定。
- 単語別統計取得。

## 8. データモデル案

以下は会話で確定した概念を表す論理モデルであり、型、インデックス、削除制約、Better Authの物理テーブル名は詳細設計で確定する。

### 8.1 認証関連テーブル

- Better Authが必要とするユーザー、アカウント、セッション、検証情報のテーブルを使用する。
- アプリ固有テーブルはBetter AuthのユーザーIDを外部キーとして参照する。
- Better Authの物理スキーマは採用バージョンに合わせ、手作業で推測しない。

### 8.2 `words`

| カラム | 必須 | 内容 |
|---|---:|---|
| `id` | Yes | 単語ID |
| `user_id` | Yes | 所有ユーザーID |
| `term` | Yes | 主に英単語 |
| `hint` | No | テスト時のヒント |
| `created_at` | Yes | 作成日時 |
| `updated_at` | Yes | 更新日時 |

関係:

- ユーザー1人に対して単語は0件以上。
- 単語1件に対して意味は1件以上。
- 単語1件に対して回答履歴は0件以上。

### 8.3 `word_meanings`

| カラム | 必須 | 内容 |
|---|---:|---|
| `id` | Yes | 意味ID |
| `word_id` | Yes | 対象単語ID |
| `meaning` | Yes | 日本語の意味 |
| `created_at` | Yes | 作成日時 |

必要に応じて `updated_at`、表示順、正規化済み文字列を追加する。ただし保存時に正規化済み値を保持するか、判定時に生成するかは未確定とする。

### 8.4 `test_results`

| カラム | 必須 | 内容 |
|---|---:|---|
| `id` | Yes | 回答履歴ID |
| `user_id` | Yes | 回答ユーザーID |
| `word_id` | Yes | 出題単語ID |
| `answer` | Yes | ユーザーが入力した回答 |
| `is_correct` | Yes | 最終的な正誤 |
| `judge_type` | Yes | `exact` / `normalized` / `ai` |
| `hint_used` | Yes | ヒント利用有無。既定値は `false` |
| `created_at` | Yes | 回答日時 |

AI判定の手動修正を採用する場合は、次を追加する。

| カラム | 必須 | 内容 |
|---|---:|---|
| `ai_judgement` | No | AIが返した元の判定 |
| `final_judgement` | Yes | ユーザー修正後を含む最終判定 |

`is_correct` と `final_judgement` の重複を避ける最終スキーマは、手動修正機能の採否と合わせて確定する。

### 8.5 追加候補

- テスト終了結果や「間違えた単語だけ再テスト」を正式採用する場合、`test_sessions` とセッションIDの追加を検討する。
- 単語の重複警告を正式採用する場合、ユーザー単位の正規化済み単語に対する一意制約または検索用インデックスを検討する。
- 集計値はまず履歴から算出する。性能上必要になった時点で集計カラムやキャッシュを検討する。

## 9. MVP

### 9.1 MVPに含める決定済み機能

- Better Auth + Google OAuthによるログイン。
- ログイン必須とユーザーごとのデータ分離。
- 単語の登録、一覧、編集、削除。
- 英単語1件に対する1件以上の日本語の意味。
- 任意のヒント登録とテスト時の表示。
- 翻訳ボタンによる意味入力支援。
- 完全ランダムと苦手優先の2つの出題方式。
- 日本語の意味を文字入力するテスト。
- 完全一致、正規化一致、AI意味判定の3段階判定。
- 回答履歴とヒント利用有無の保存。
- 単語別の正解率、正解数/回答数の表示。
- 正解率に応じたカード色。未回答は白。
- Cloudflare Workers + D1を中心とした構成。
- 将来のChrome拡張が同じHono APIを利用できる境界設計。

### 9.2 MVP追加候補（要確認）

次は会話内でMVPへの追加が提案されたが、ユーザーによる明示的な最終確定が必要である。

- 単語検索。
- 重複登録チェックと既存単語編集への導線。
- テスト終了結果画面。
- 間違えた単語だけ再テスト。
- AI判定をユーザーが正解/不正解へ手動修正する機能。

## 10. 将来機能

### 10.1 Version 1.x候補

- CSV/JSONインポート・エクスポート。
- 英単語の発音再生。
- 学習統計ダッシュボード。
- 正解率、回答回数、最近の正誤を組み合わせた苦手度アルゴリズム。
- ノーヒント正解率。

### 10.2 Chrome拡張

Chrome拡張は将来実装する前提で、MVPのAPI境界を設計する。

想定フロー:

```text
Webページ上で英単語を選択
  ↓
「Tangoに登録」を実行
  ↓
選択単語を登録フォームへ渡す
  ↓
自動翻訳で意味候補を取得
  ↓
必要ならヒントを入力
  ↓
確認して登録
```

将来の拡張要件:

- Webアプリと同じユーザーアカウント・単語データを利用する。
- Hono REST APIを利用する。
- 選択文字列を取得し、Tangoへの登録操作を表示する。
- 自動翻訳後もユーザーが意味を確認・編集してから保存する。
- 拡張側の認証方式、権限、CORS、Cookieまたはトークン連携は未確定とする。

### 10.3 その他の将来候補

- 例文。
- 品詞。
- 発音記号。
- スマートフォンアプリ。

## 11. 非機能・品質要件

### 11.1 セキュリティ

- すべての単語・履歴操作で認証と所有者チェックを行う。
- 翻訳・AI APIの秘密情報をブラウザーへ露出しない。
- 入力値を検証し、表示時のインジェクションを防ぐ。
- 削除・更新対象はセッション上のユーザーが所有するデータに限定する。

### 11.2 使いやすさ

- 単語帳らしいカードUIとする。
- 色に加えて数値・テキストでも学習状況を伝える。
- AI判定を使った場合は明示し、判定の透明性を確保する。
- 翻訳結果は自動保存せず、ユーザー確認を必須とする。

### 11.3 保守性

- WebとChrome拡張から共通利用できるAPIを用意する。
- 翻訳プロバイダーとAI判定プロバイダーを交換可能にする。
- 正規化処理と正誤判定処理をUIから分離し、単体テスト可能にする。
- DBアクセスはDrizzleのスキーマとマイグレーションで管理する。

### 11.4 テストで最低限確認する事項

- ユーザー間で単語と履歴が漏れない。
- 意味を0件では保存できない。
- 複数意味のどれか1つに一致すれば正解になる。
- 完全一致がAI判定を呼ばずに正解になる。
- 正規化一致がAI判定を呼ばずに正解になる。
- 上記に不一致の場合だけAI判定を呼ぶ。
- ヒント未登録時はヒントボタンを表示しない。
- ヒント利用有無が回答履歴へ保存される。
- 未回答カードは白で、0%回答済みカードと区別される。
- 翻訳実行だけでは単語がDBへ保存されない。

## 12. 未確定事項

実装前、または該当機能へ着手する前に次を決める。

1. 翻訳に使うプロバイダー、モデル、料金上限、候補件数。
2. AI正誤判定に使うプロバイダー、モデル、プロンプト、再試行、タイムアウト、障害時の扱い。
3. 正規化対象の詳細。句読点・記号・かな/カナ・長音・表記ゆれを含めるか。
4. テストの出題数、既定値、同一セッション内の重複出題可否。
5. 苦手優先モードでの未回答単語と回答回数の重み。
6. カード色の正確なカラーコード、補間方式、アクセシビリティ基準。
7. 単語重複の定義。大文字小文字や全角半角を同一単語とみなすか。
8. 単語削除時に回答履歴を削除、保持、匿名化のどれにするか。
9. テスト終了結果、間違いのみ再テスト、検索、重複警告をMVPへ含めるか。
10. AI判定の手動修正をMVPへ含めるか。含める場合の監査履歴とデータモデル。
11. Chrome拡張の認証連携、権限、配布方法。
12. 各ライブラリのバージョンとCloudflareデプロイ方式。

## 13. Codexへの実装引き継ぎ方針

- この文書の「決定済み」と「MVPに含める決定済み機能」を実装対象の基準とする。
- 「要確認」「未確定事項」は、勝手にプロダクト仕様として固定しない。
- 未確定でも内部実装上の仮置きが必要な場合は、変更しやすい境界を設け、仮定を別途記録する。
- 実装前に採用技術の現行バージョンと相互互換性を公式情報で確認する。
- まず認証・データ分離・単語CRUD・複数意味のデータモデルを成立させ、その後にテストとAI/翻訳を追加する。
- 正誤判定は、完全一致と正規化一致を決定的なローカル処理として先に実装し、AI呼び出しを最後のフォールバックにする。
- Chrome拡張はMVPで実装しないが、Web固有のサーバー関数だけにドメイン処理を閉じ込めず、Hono APIとサービス層から再利用できるようにする。

## 14. MVP受け入れ条件

MVPは、少なくとも次が一連の操作として成功した時点で成立とみなす。

1. ユーザーがGoogleでログインできる。
2. 英単語、複数の日本語の意味、任意のヒントを登録できる。
3. 再ログイン後も自分の単語が表示され、他ユーザーの単語は表示されない。
4. 翻訳ボタンで意味候補を得られ、編集してから保存できる。
5. 完全ランダムと苦手優先のどちらかを選んでテストを開始できる。
6. ヒント登録済み問題では、ボタンを押した場合だけヒントが表示される。
7. 複数意味のうち1件と完全一致または正規化一致すれば正解になる。
8. 文字列一致しない回答だけAI判定へ送られる。
9. 正誤、判定種別、回答、ヒント利用有無が履歴へ保存される。
10. 単語一覧で正解率と正解数/回答数を確認できる。
11. 回答済みカードは正解率に応じて赤から黄緑へ変化し、未回答カードは白で表示される。
